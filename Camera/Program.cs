using System;

namespace LeopardCamera
{
    class Program
    {
        static void Main(string[] args)
        {
            //CameraInfo camInfo = new CameraInfo
            //{
            //    CameraType = CameraTypeEnum.Manta,
            //    SerialNumber = "50-0503320578",
            //};

            //Camera camera = Camera.CameraInit(ref camInfo);
            //camera.Open();
            
            //Console.WriteLine("Supporting extension? {0}", camera.isSupportUVCExt);
            //if (camera.isSupportUVCExt)
            //{
            //    IUVCExtension uvcExtension = camera as IUVCExtension;
            //    Console.WriteLine("Sharpness = {0}", uvcExtension.CalcSharpness());
            //}

          


            Console.ReadLine();
        }
    }
}
